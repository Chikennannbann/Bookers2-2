<div class='container px-5'>
  <div class='row'>
    <h2>Followings</h2>
    <% if @users.exists? %>
      <%= render 'users/index', users: @users %>
    <% else %>
      <p>フォローしていません</p>
    <% end %>
  </div>
</di